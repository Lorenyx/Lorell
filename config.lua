return {
    protocol = "LORELL",
    timeout = 30,
    -- Below should not be used directly
    --> script to rename client.lua to
    _SCRIPT = "lorell.lua",
    _CONFIG = "config.lua",
    --> notice of update needed
    _UPDATE = ".UPDATE_LORELL",
    --> name of token file
    _TOKEN = ".TOKEN_LORELL"
}